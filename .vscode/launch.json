{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "python",
            "request": "launch",
              "name": "NxOpen-Debugging",
            "program": "${workspaceFolder}/tests/test_part.py",
            "cwd": "${workspaceFolder}"
        }
    ]
}